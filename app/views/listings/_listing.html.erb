<tr>
  <td><%= listing.title %></td>
  <td></td>

  <td><%= link_to 'Edit', edit_listing_path(listing), class: 'btn btn-warning' %></td>
  <td><%= link_to 'Delete', listing_path(listing), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'  %></td>
</tr>